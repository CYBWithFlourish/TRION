import React from 'react';
import { GlobalWrapper } from './GlobalWrapper';
import { CreateListingForm } from './CreateListingForm';

export const ListIntentPage = () => {
    return (
        <GlobalWrapper>
            <CreateListingForm />
        </GlobalWrapper>
    );
};
